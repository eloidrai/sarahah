<!DOCTYPE html>
<html dir="ltr" lang="fr">
  <head>
    <meta charset="utf8"/>
    <title>Inscription</title>
    <%- include("style.ejs") %>
  </head>
  <body>
    <%- include("header.ejs") %>
    <section>
        <h2>S'inscrire</h2>
        <form method="post" action="">
          <input type="hidden" name="_csrf" value="<%= token %>">
          <input name="nom" placeholder="Nom" minlength=4 maxlength=15/>
          <input type="password" name="mdp" placeholder="Mot de passe" minlength=6 maxlength=12/>
          <input type="submit" value="Valider"/>
        </form>
        <p class="err"><%= ["", "Formulaire invalide", "Erreur base de données (nom déjà présent)"][msg] %></p>
    </section>
  </body>
</html>
